int seed(int x)
{
	return x*2;
}
int main()
{
	int z=1;
	auto f=[=](auto &self,int n,int (*p)(int))->int
	{
		if(!n)
			return p(z);
		return self(self,n-1,[=](int x)->int{return p(x+n+z);});
	};
	return f(f,3,seed);
}
