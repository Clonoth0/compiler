int apply(int (*f)(int),int x,int n)
{
	int result=x;
	int i=0;
	while(i<n)
	{
		result=f(result);
		i=i+1;
	}
	return result;
}
int main()
{
	auto inc=[](int x)->int{return x+1;};
	return apply(inc,0,10);
}
